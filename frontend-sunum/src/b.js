/*import React from "react";

<a class="nav-link" href="#" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="true" aria-controls="collapseTwo">
    <i class="fas fa-fw fa-cog"></i>
    <span>Components</span>
</a>

function RadioButtonGroup(props) {
    return (
        <div>
            <label>
                <input type="radio" value="Constant GUI" name="flexRadioDefault" checked={props.selectedOption === 'Constant GUI'}onChange={props.handleChange}onClick={props.handleChange} checked/>
                Constant GUI
            </label>
            <br />
            <label>
                <input type="radio" value="Pop-up GUI" name="flexRadioDefault" checked={props.selectedOption === 'Pop-up GUI'} onChange={props.handleChange} onClick={props.handleChange} />
                Pop-up GUI
            </label>
        </div>
    )
}
}
function handleChangeRatio(event) {
    console.log(event.target.nodeName)
    setSelectedOption(event.target.value);
}
<TrackInfo ID={ID} Latitude={Latitude} Longitude={Longitude} Unit={Unit} markers={markers}> </TrackInfo>








<p style={{color:'white'}}>Connection List </p>

                    <div id="myModal2" className="modal"  style={{display:'inline-block',left: '84.5%',position: 'fixed',top: '70%',height:'200px',width:'300px'}}>
                        <ul className="list-group">
                            {
                                connections.map(item => <li className="list-group-item">{item}</li>)

                            }
                        </ul>
                    </div>






*/